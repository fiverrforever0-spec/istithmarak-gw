## مرحلة لوحة التحكم
- server.js: API محلي يدير الأرقام والرسائل.
- index.html: لوحة تحكم ويب للإدارة.
- data.json: تخزين الأرقام والرسائل.


---

## مرحلة لوحة التحكم والسيرفر (مكتملة)

### التاريخ
2026-08-17

### المكونات
- خادم API محلي (Node.js + Express) يعمل على المنفذ 3000.
- لوحة تحكم ويب فاتحة واحترافية.
- تخزين الأرقام والرسائل في ملف data.json.
- إضافة رقم واحد أو عدة أرقام (لصق مباشر).
- استيراد ملف CSV.
- حذف رقم.
- عرض سجل الأرقام والرسائل.
- حفظ نصوص الرسائل الفورية والتذكيرية.
- نسخ احتياطي تلقائي يومي.

### المسارات
- السيرفر: `~/istithmarak-gateway-backend/server.js`
- اللوحة: `~/istithmarak-gateway-backend/index.html`
- البيانات: `~/istithmarak-gateway-backend/data.json`

### ملاحظات
- واجهة اللوحة أصبحت فاتحة ومتجاوبة مع الموبايل بدون تداخل.
- استيراد CSV تم بنجاح.
- حذف الأرقام تم بنجاح.
- لا توجد قاعدة بيانات SQLite حالياً، نستخدم JSON مع نسخ احتياطي.

### الخطوات القادمة المقترحة
- ربط التطبيق برصد المكالمات الفائتة تلقائيًا.
- إرسال SMS حقيقي من التطبيق.
- جدولة التذكير تلقائيًا.
- نشر السيرفر على VPS بدلاً من Termux عند الحاجة.
